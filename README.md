# Expense Tracker

> Single page application for tracking expenses.

---

### Setup & Installation

1.  **Dependencies**
    Enter both `backend` and `frontend` folders:
    ```bash
    npm install
    ```
3.  **Environment**
    Duplicate `.env.example` as `.env` in both folders and set your `PORT` on the backend and the URL of your localhost for your frontend, i.e: http://localhost:3000

4.  **Run**
   On the backend folder:
       ```bash
    npm run start:dev
    ```
   And on the frontend folder:
       ```bash
    npm run dev
    ```
    Finnaly access the **localhost** port where the frontend is running.

---

### Key Features

* **View** example expenses.
* **Create** new entries.
* **Update** existing expenses.

---

**Built with React, Next.js, NestJS & TypeScript**
